# Azure Function App

This Java 17 Azure Function exposes `POST /api/process-order`. Run it locally with Azure Functions Core Tools after copying `local.settings.json.example` to `local.settings.json`.